apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: cloud-native-ai-hrms
spec:
  type: ClusterIP
  selector:
    app: employee-service
  ports:
    - protocol: TCP
      port: 4002
      targetPort: 4002
